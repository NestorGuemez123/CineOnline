@model VideoOnDemand.Web.Models.PaginatorViewModel<VideoOnDemand.Web.Models.ThumbnailMovieViewModel>


@{
    ViewBag.Title = "View";
}

<h2>Administrar Películas</h2>

<p>
    @using (Html.BeginForm())
    {
        <div class="form-inline">
            <input type="text" id="page" name="page" value="1" hidden />

            <div class="col-md-3">
                <input type="text" id="busqueda" name="busqueda" class="form-control" placeholder="Nombre de la Película" value=@ViewBag.Busqueda>
                <button type="submit" formmethod="get" class="btn" style="background-color:#e50914; color:white; margin-top:-22% ;margin-left:102%">Buscar</button>
            </div>
        </div>
    }
    <div><h1></div>

    <br />
    <br />
<h5>
    @Html.ActionLink("Crear nueva Película", "Create")
</h5>
<table class="table table-hover table-responsive">
    <tr>
        <th>
            Nombre
        </th>

        <th>
            Duración (min)
        </th>

        <th>
           Fecha de lanzamiento
        </th>
        <th>
            Estado para los usuarios
        </th>

        <th>Acciones</th>
    </tr>

    @foreach (var item in Model.Results)
    {
        <tr>
            <td>
                @item.Nombre
            </td>

            <td>
                @item.DuracionMin
            </td>

            <td>
                @item.FechaLanzamiento.Value.ToString("dd/MM/yyyy")
            </td>
            <td>
                @item.EstadosMedia
            </td>
            <td>
                @Html.ActionLink("Editar", "Edit", new { id = item.MediaId }) |
                @Html.ActionLink("Eliminar", "Delete", new { id = item.MediaId })
            </td>
        </tr>
    }
    </table>


    <h1></h1>

    @section Scripts {
        @Scripts.Render("~/bundles/jqueryval")
    }


